import { DateTime } from "ionic-angular";

export interface InterfaseEvent {
Event: string , 
computer: string,
Operator:string, 
source:string, 
startTime:DateTime,  
endTime :DateTime,  
message:string,  
eventColor:string 
}
